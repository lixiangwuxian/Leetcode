#include "tool.h"
using namespace std;

class Solution {
public:
    string replaceSpace(string s) {
        string result;
        int i;
        for (i = 0; i < s.length(); i++) {
            
        }
    }
};